# 0.2.65 스티커·이모지 선택 창 이름 복원 (2026-09-08)

- 실제 Chrome에서 이모지 숨김 설정이 data-type=emoji 선택 버튼까지 display:none으로 만들고, 스티커·이모지 이름의 ::after에 Discord의 opacity:0이 남는 것을 확인했습니다. 스티커 그림의 빈 고정 높이 래퍼도 이름 공간을 차지했습니다.
- 이름을 붙인 선택 버튼은 이모지 숨김 대상에서 제외하고 그림만 숨깁니다. 대체 이름의 투명도와 배치를 명시하고 빈 그림 래퍼를 숨기되 접근성 설명은 유지합니다. 원래 선택 버튼의 좌표·크기·비활성 상태·이벤트는 보존합니다.
- 서버 카테고리 제목은 이름 대체 처리에서 제외해 제목이 두 번 나오는 현상도 수정했습니다.
- 실제 구조를 반영한 합성 Chrome 샘플에서 스티커 130×80px, 이모지 80×40px 선택 영역과 불투명한 이름 표시를 확인했습니다. 스티커·이모지 이름 클릭은 각각 원래 선택 처리로 전달됐습니다. TypeScript 및 자동 테스트 96개 통과. 실제 대화에서는 항목을 선택하거나 전송하지 않았습니다.
- 실제 Chrome에 적용 후 스티커 96×96px, 이모지 48×48px 선택 영역에서 이름 표시와 그림 숨김을 확인했습니다. 이름의 불투명도는 1이며 서버 카테고리 제목에 중복 이름 표식이 없었습니다.
- 이모지 표시를 켜면 이름 표식이 제거되고 커스텀 이미지가 40×40px로 복원됩니다. 이미지 숨김의 예외에 선택기 내부 이모지 그림도 포함해 두 설정이 충돌하지 않게 했습니다. 다시 끄면 그림이 숨겨집니다. 원래 이모지 숨김 설정으로 복원하고 선택 창을 닫았습니다. 실제 계정에서는 전송·반응 추가·스티커 선택을 하지 않았습니다.

# 0.2.64 스레드 앱 배지 서식 수정 (2026-09-08)

- 실제 Chrome 스레드 최근 메시지에서 botTag 배지에 Discord 기본 파란 배경(rgb(88,101,242))이 남아 있는 것을 확인했습니다. 배지 식별 문구와 접근성 이름을 유지하면서 옅은 초록 배경, 얇은 사각 테두리, 16px 높이로 통일했습니다.
- 첨부파일 미리보기의 기울임꼴을 제거하고 긴 안내는 한 줄 말줄임으로 표시합니다. 뒤쪽 첨부 아이콘은 12px로 정렬합니다.
- 실제 구조를 합성한 로컬 Chrome 샘플에서 배지 배경 rgb(237,244,239), 높이 16px를 확인했습니다. 1353px 창과 420px 창 모두 스레드 카드 높이는 57px였습니다. 420px에서 카드 오른쪽 끝은 409px였으며 다음 메시지가 현재 행 끝 376px에서 시작해 겹치지 않았습니다.
- 샘플에서 원래 화면으로 복원하면 카드 표식이 제거되고 배지도 원래 파란 배경으로 돌아왔습니다. 임시 창 크기는 복원했습니다. TypeScript 검사와 자동 테스트 95개 통과.
- 실제 계정에서는 수정 전 원인과 DOM을 확인했습니다. 사용자의 확장 재로드 후 기존 탭은 브라우저 도구 연결이 끊겼고, 동일 채널을 연 새 확인 탭은 재로그인 화면으로 이동해 수정본의 실제 계정 적용 확인을 마치지 못했습니다. 실제 대화 내용은 샘플과 문서에 저장하지 않았습니다.

# 0.2.63 확장 재로드 후 종료 오류 수정 (2026-09-08)

- 사용자가 제공한 스택의 chrome.storage.onChanged 접근은 controller.destroy에서 호출하는 구독 해제 함수였습니다. 확장 재로드로 chrome.storage가 사라진 이전 페이지에서 전역 API를 다시 조회해 TypeError가 발생했습니다.
- 등록 당시의 이벤트 객체로 구독을 해제하고 반복 해제를 무시합니다. 이미 무효화된 이벤트가 제거 시 예외를 던져도 페이지 종료 정리를 마칩니다. 해제 뒤 남은 콜백도 설정을 적용하지 않습니다.
- 구독 등록 단계의 연결 실패도 컨트롤러 시작 처리에서 잡아 페이지 새로고침을 안내합니다. 설정을 읽던 중 페이지가 종료되면 뒤늦게 오류 안내를 다시 생성하지 않으며, 확장 팝업도 구독 등록 실패를 안내합니다.
- 저장 API가 사라지는 경우, 이벤트 제거가 context invalidated를 던지는 경우, DOM 적용 후 반복 종료, 초기 구독 실패, 읽기 도중 페이지 종료를 재현 테스트합니다. TypeScript 및 자동 테스트 95개 통과. 실제 사용 중인 브라우저의 확장을 일부러 무효화하는 점검은 수행하지 않았습니다.

# 0.2.62 실제 Chrome 스레드·스티커 점검 (2026-09-08)

- 로그인된 실제 Discord의 스레드 메시지에서 확인했습니다. 0.2.61의 작성자·시간이 —로 나오던 원인은 실제 li 안의 article도 메시지 선택자와 일치하는 구조였습니다. 소유 메시지 판정을 수정하고 실제 작성자·시간 표시 복원을 확인했습니다.
- 실제 스레드 제목은 threadName 대신 topLine 안의 name 요소였습니다. 원래 스레드 버튼과 이 구조를 함께 인식하고, threadMessageAccessoryPreview 및 중첩된 최근 메시지 행을 엑셀 스타일로 정리했습니다. 서버 배지는 첨부 이미지 처리에서 제외했습니다.
- 사진에서 길게 늘어진 인용은 숨김 설명이 아니라 실제 본문이었습니다. 원문을 변경하지 않고 긴 스레드 시작 본문을 세 줄로 접으며 내용 펼치기·접기를 제공합니다. 실제 본문 높이 60px → 360px → 60px 전환, 다음 메시지 시작 위치가 현재 행 아래 끝과 일치하는 것을 확인했습니다.
- 실제 hasThread 요소의 ::after 연결선이 펼치기 버튼 중앙을 덮고 있었습니다. 이 장식과 별도 spine을 제거했고 ::after의 content:none 및 버튼 클릭을 확인했습니다. 본문 확장 상태는 가상 목록 재삽입에서 유지하며 대화 이동 시 초기화합니다.
- 실제 Chrome 1353px 폭에서 스레드 카드 높이 57px, 셀 안쪽 표시를 확인했습니다. 420px 폭에서 카드 너비 239px, 오른쪽 끝 399px(셀 오른쪽 끝 410px), 높이 57px, 본문 60px였습니다. 리본 clientHeight와 scrollHeight는 모두 79px로 세로 넘침이 없었습니다.
- 실제 스티커 펼침은 160×160px, 메시지 행은 212px였으며 다음 행과 겹치지 않았습니다. 펼치기에는 정보창이 열리지 않았고 그림 클릭으로만 정보가 열렸습니다. Esc로 정보창을 닫은 뒤 다시 접기가 동작했습니다.
- 프로필 표시 켜기·끄기, 이모지 숨김 복원, 파일→서버 목록과 삽입→채널 목록 분리, 채널 창을 접은 상태에서 13개 채널 목록 유지를 확인했습니다. 표시 설정은 프로필·이모지 숨김 및 채널 창 접힘으로, 창 크기는 원래 1353px로 복원했습니다. 펼친 본문·미디어도 모두 접었습니다.
- 메시지 전송·반응 추가·첨부 업로드는 수행하지 않았습니다. 이번에 서버/DM 이동, 한글 입력·전송, Edge 전체 회귀는 점검하지 않았습니다. 실제 대화 내용과 계정 식별자는 테스트·문서에 저장하지 않았으며 구조만 합성 샘플에 반영했습니다.
- TypeScript 및 자동 테스트 91개 통과. 실제 설치 위치는 사용자가 지정한 artifacts/sheetcord-0.2.61이며 폴더 이름을 유지한 채 0.2.62 파일로 갱신했습니다.

# 0.2.61 스레드 인용·엑셀 스타일 검증 (2026-09-08)

- 스레드 참조를 흰색 배경, 얇은 셀 테두리, 초록색 제목으로 표시합니다. 제목·메시지 수와 최근 메시지를 두 줄로 정리하고 원래 스레드 클릭을 유지합니다. 긴 제목·미리보기는 셀 안에서 말줄임합니다.
- 화면 읽기용 숨김 설명은 1px 클리핑을 유지하며 이모지·이미지 처리 대상으로 따로 복제하지 않습니다. 부모 메시지의 시간·작성자를 읽을 때 설명·답장·스레드 미리보기를 제외합니다.
- 로컬 Chromium 재현 샘플에서 320px 창의 카드 너비 148px·높이 57px, 다음 행과 간격 8px를 확인했습니다. 1280px 창에서 카드 너비 274.75px·높이 57px, 다음 행과 간격 10px였고 두 경우 모두 셀 밖 넘침이 없었습니다.
- 프로필·이모지 표시를 켜고 끌 때 높이가 유지됐고 스레드 클릭이 원래 핸들러에 전달됐습니다. 숨김 설명은 1px와 inset(50%) 클리핑을 유지했습니다. 표시 설정과 브라우저 크기 임시 설정을 복원했습니다.
- 자동 테스트 88개 및 TypeScript 검사 통과. 메타데이터 분리, 숨김 설명 처리, 전체 카드 이모지 복제, 네이티브 클릭, 카드 재사용과 해제 복원을 포함합니다. 연결된 브라우저에 실제 Discord 탭이 없어 이번 결과는 사진을 참고한 재현 샘플 기준이며 실제 해당 스레드 확인은 남아 있습니다.

# 0.2.60 채널 수집·메뉴 분리 검증 (2026-09-08)

- 서버 목록은 파일 메뉴, 채널 목록은 삽입 메뉴로 분리했습니다. 각 부모 메뉴에 해당 목록만 있는지 자동 테스트와 로컬 Chromium에서 확인했습니다.
- 한 번에 5개만 렌더링하는 30개 채널 샘플에서 전체 항목을 원래 순서대로 수집했습니다. 바깥 작업 창과 안쪽 목록이 모두 스크롤되는 구조에서도 안쪽 목록을 수집하며 원래 scrollTop 400으로 복원했습니다.
- 채널 창을 접은 상태에서 삽입 메뉴에 전체 목록이 유지되고 마지막 채널 이름으로 검색되는 것을 확인했습니다. 화면 밖 원본 링크가 제거된 마지막 채널을 선택해 `/channels/100/9029`로 이동하는 것을 로컬 Chromium과 자동 테스트로 확인했습니다.
- 자동 테스트 84개와 TypeScript·빌드·ZIP 검증 통과. 수집 순서, 중첩 스크롤, 서버 이동 중 취소, 해제 즉시 위치 복원과 재수집을 포함합니다. 현재 결과는 로컬 재현 샘플 기준이며 실제 Discord의 Chrome·Edge 적용 검증은 남아 있습니다.
- 접힌 카테고리는 자동으로 펼치지 않습니다. 목록 수집 중 사용자가 원래 목록을 조작하면 중단하며, 메뉴를 다시 열어 재수집할 수 있습니다. 채널 정보는 현재 서버에 한해 메모리에서만 유지합니다.

# 0.2.59 스티커 펼치기·접기 클릭 분리 검증 (2026-09-08)

- 펼치기·접기 버튼을 스티커의 가장 바깥쪽 클릭 영역 밖에 놓고, 클릭·포인터·키 이벤트가 원래 스티커 핸들러로 전달되지 않도록 처리합니다. 스티커 그림의 원래 이벤트는 유지합니다. 일반 텍스트 버튼으로 꾸미는 처리에서도 스티커를 제외합니다.
- 중첩된 스티커 버튼, 클릭 캡처 핸들러, 32px 내부 이미지와 축소 변환을 재현했습니다. 로컬 Chromium에서 펼친 영역과 그림 모두 160 × 160px, 메시지 행은 212px였습니다. 그림의 아래 끝은 470px, 다음 행은 480px에서 시작해 겹침이 없었습니다.
- 펼치기를 눌렀을 때 정보 창이 생기지 않았고, 그림을 누르면 스티커 정보가 열렸습니다. 정보 창을 닫고 접기를 눌렀을 때 다시 정보가 열리지 않았으며 행 높이는 39px로 돌아왔습니다.
- 자동 테스트 80개, TypeScript·빌드·ZIP 검증 통과. 현재 연결된 브라우저 목록에는 실제 Discord 탭이 없어 설치된 탭 새로고침과 실제 계정 검증은 수행하지 못했습니다.

# 0.2.58 채널 창 숨김·스티커 높이 회귀 검증 (2026-09-08)

- 숨긴 채널 창의 렌더링 크기를 유지하되 투명도·클리핑·포인터 차단·inert로 화면 표시와 직접 조작을 막습니다. 목록이 비워질 경우 현재 서버에서 확인한 항목을 메모리에서 유지하며 서버 변경·확장 해제 시 초기화합니다.
- ResizeObserver가 0px 채널 창의 항목을 제거하는 샘플을 추가했습니다. 좁은 창에서 링크 8개를 유지했고, 1280px에서 채널 창을 접었을 때 원본 영역은 204 × 434px, 투명도 0으로 유지됐습니다. 삽입 메뉴에서 개발 논의(`/channels/100/1004`)로 이동했습니다.
- 일반 클래스의 160px 스티커 래퍼, 절대 위치 내부 요소, 180px 메시지 행 높이를 재현했습니다. 접힌 행은 일반 메시지와 같은 39px였고 펼친 행은 242px로 늘어났습니다. 캔버스 아래 끝 470px, 다음 행 시작 510px로 겹침이 없었으며 전체 접기 후 39px로 돌아왔습니다.
- 자동 테스트 79개, TypeScript·빌드·ZIP 검증 통과. 캐시된 채널 클릭, 다른 서버 목록 혼입 방지, 추가 문구가 있는 미디어 컨테이너 보존과 DOM 복원을 포함합니다. 이번 결과는 로컬 재현 샘플 기준이며 실제 Discord의 해당 화면 확인은 남아 있습니다. 임시 브라우저 크기를 복원했습니다.

# 0.2.57 이미지 표시 스위치 제거 검증 (2026-09-08)

- 리본·설정창의 이미지 표시 스위치와 저장 설정을 제거했습니다. 기존 `showImages: true` 값도 무시하며, 사진·GIF·스티커는 접힌 상태에서 개별로 펼칠 수 있습니다. 전체 접기는 화면 밖으로 스크롤된 항목의 펼침 기록도 초기화합니다.
- 자동 테스트 76개 통과. 기본 접힘, 개별 펼치기, 전체 접기, 새 미디어 추가, 가상 목록 재삽입, 채널 이동 초기화, 스포일러 미공개 유지, 이모지 대체 본문 안에서의 펼치기, 원래 DOM 복원, 설정 이전을 포함합니다.
- 로컬 Chromium에서 이미지는 실제 `display: block`, 스티커 캔버스는 `display: inline`으로 나타났고 행 높이는 각각 152px·212px였습니다. 다음 행과 겹침이 없었으며 전체 접기 후 두 항목과 일반 메시지 모두 39px로 돌아왔습니다. GIF·스티커 선택 목록의 이름 표시는 유지합니다.
- TypeScript 검사·빌드·ZIP 검증 통과. 실제 Discord Chrome·Edge 설치 상태 검증은 남아 있습니다. 임시 브라우저 크기는 확인 후 복원했습니다.

# 0.2.56 삽입 메뉴·스티커 식별·고정 버튼 검증 (2026-09-08)

- 상단은 파일·홈·삽입·페이지 레이아웃 순서로 복원하고, 서버·채널 목록은 삽입 메뉴 안으로 옮겼습니다. 샘플에서 삽입 → 채널 목록 → 개발 논의 이동(`/channels/100/1004`)을 확인했습니다.
- 320px 폭의 하위 목록은 좌우 8px 여백 안에 표시됐고 메뉴 표시 높이·스크롤 높이는 모두 23px였습니다. Esc로 닫으면 삽입 버튼으로 포커스가 돌아옵니다.
- 일반 이미지 래퍼 내부의 스티커 주소, 스티커 클래스의 대소문자, 상위 버튼의 이름표와 이름 변경을 처리합니다. 샘플에서 `[스티커: 춤추는 고양이]`를 확인했습니다. 스포일러 안의 이름은 숨기며 일반 사진을 스티커로 분류하지 않는 회귀 테스트를 포함합니다.
- 인라인 파란색을 가진 고정 메시지 버튼의 배경은 `rgb(243,246,244)`, 글자는 `rgb(33,115,70)`으로 바뀌었습니다. 원래 DOM과 설정 해제 복원은 자동 테스트로 확인했습니다.
- 자동 테스트 74개 및 TypeScript·빌드·ZIP 검증 통과. 이번 결과는 로컬 샘플 검증이며 실제 Discord의 최신 DOM 검증은 남아 있습니다. 임시 브라우저 크기는 검증 후 복원했습니다.

# 0.2.55 미디어 한 줄 표시·선택 목록 검증 (2026-09-08)

- 고정 높이 280px 이미지 격자, 160px 캔버스 스티커, 64px 큰 이모지를 샘플에 재현했습니다. 숨긴 상태에서 세 메시지와 일반 한 줄 메시지의 행 높이가 모두 39px였고 행 겹침은 없었습니다. 미디어 내부 표시 줄 높이는 20px였습니다.
- 로컬 Chromium에서 이미지 표시를 켜면 이미지·스티커 행이 299px·179px로 복원되고 이모지 이름 표시는 유지됐습니다. 이모지 표시를 켜면 해당 행은 83px로 복원되고 GIF·스티커 목록의 이름 표시는 유지됐습니다. 두 설정을 다시 끄면 네 행이 모두 39px로 돌아왔습니다.
- GIF·스티커·이모지 목록의 이름 표시, 원래 선택 항목 클릭, 비활성 항목 클릭 방지, 이름 변경·재사용·추가·삭제, 설정 전환과 원래 DOM 복원을 자동 테스트합니다. 로컬 브라우저에서 `인사하는 고양이` 항목 선택도 확인했습니다. 실제 메시지는 전송하지 않았습니다.
- 자동 테스트 72개, TypeScript 검사 및 빌드·ZIP 검증 통과. 실제 Discord의 최신 선택기 구조 및 Chrome·Edge 설치 상태 검증은 남아 있습니다. 브라우저 크기 임시 설정은 확인 후 해제했습니다.

# 0.2.54 상단 탐색 메뉴 검증 (2026-09-08)

- 홈 옆에 서버 목록·채널 목록을 추가했습니다. 이름 검색, 현재 선택, 서버 읽지 않은 표시, 최신 원본 요소 클릭, 메뉴 간 전환과 해제 시 정리를 자동 테스트합니다.
- 로컬 Chromium 샘플에서 서버 검색 후 `/channels/200/1000`, 채널 검색 후 `/channels/200/1003`, 개인 메시지 선택 후 `/channels/@me` 이동을 확인했습니다.
- 320px 폭에서 두 메뉴 버튼의 오른쪽 끝은 각각 155.30px, 226.59px였습니다. 펼친 목록은 좌우 8px 여백 안에 표시됐으며 상단 메뉴의 표시 높이와 스크롤 높이는 모두 23px로 세로 넘침이 없었습니다. Esc 닫기와 화면 크기 복원도 확인했습니다.
- TypeScript 검사, 자동 테스트 67개 및 빌드·ZIP 검증 통과. 이번 결과는 샘플 DOM·브라우저 검증이며 실제 Discord의 Chrome·Edge 적용은 아직 확인하지 못했습니다.

# 0.2.53 리본 표시 검증 (2026-09-08)

- 사용자 요청에 따라 네이티브 상단 받은 편지함·도움말 버튼을 Sheetcord 화면에서만 숨기고, 두 버튼을 리본 위에 고정하던 규칙을 제거했습니다.
- 리본 높이는 90px로 유지하며 버튼을 축소하지 않습니다. 채널 바로가기는 두 줄을 유지한 채 가로로 이어지므로 상단 리본에 세로 스크롤이 생기지 않습니다.
- 로컬 Chromium의 320·600·601·1280px 폭에서 리본 높이 90px, 표시/스크롤 영역 높이 모두 79px, 잘린 버튼 0개를 확인했습니다. 표시 스위치 크기는 네 폭 모두 142.58 × 26px였습니다.
- 입력창은 리본 끝보다 4px 아래에 표시되며 겹치지 않았습니다. 원래 화면에서는 두 버튼이 다시 표시되고 샘플 받은 편지함 클릭이 동작했습니다. 검증 후 임시 화면 크기 설정을 해제했습니다.
- 자동 테스트 63개, TypeScript 검사 및 빌드·ZIP 검증 통과. 이번 변경은 샘플 브라우저 검증이며 실제 Discord 적용은 아직 확인하지 못했습니다.

# 0.2.52 탭 아이콘·이름 검증 (2026-09-08)

- TypeScript 검사와 자동 테스트 63개 통과. 탭 아이콘 링크 여러 개·추가·삭제·속성 변경, 사용자 지정 제목 유지, 기존 설정 마이그레이션, 빈 이름·80자 제한, 해제 시 최신 원래 제목·아이콘 복원을 포함합니다.
- 실제 팝업 HTML과 설정 코드를 사용하는 샘플 DOM 테스트에서 이름 저장·비우기와 기존 이모지 설정 보존을 확인했습니다.
- 로컬 Chromium 샘플에서 투명 SVG 아이콘 링크 적용, `업무 정리.xlsx` 제목 변경, 새로고침 후 저장 유지, 원래 화면 전환 후 샘플 제목·아이콘 복원을 확인했습니다.
- 이번 변경의 실제 Chrome·Edge Discord 탭 표시와 사이트 콘텐츠 보안 정책 호환성은 아직 확인하지 못했습니다. 기존 0.2.51 실제 검증 결과를 이번 기능 검증으로 간주하지 않습니다.

# 0.2.51 integration verification (2026-09-08)

- TypeScript check, 56 tests, production build and archive verification pass in the main repository checkout.
- Actual Chrome/Discord: channel shortcut cells render inside the ribbon (top 73.6px, bottom 125.6px within 66–148px at 2048px viewport width).
- With the sidebar collapsed, switching from a text channel to a forum and back works; the current-channel cell follows navigation. Sidebar visibility and all three media preferences were restored. No messages were sent.
- This verifies the channel-shortcut release fix, not completion of the full Excel-fidelity audit. Narrow layouts, all popup variants and real-world performance improvement still need further verification. Only channel links mounted in the native sidebar are available as shortcuts.
- Older observations below describe earlier builds and must not be read as current comprehensive coverage.

# Sheetcord 0.1.0 검증 기록

검증일: 2026-09-07. 이 문서는 **샘플 DOM에서 확인한 동작**과 **실제 디스코드에서 아직 확인하지 못한 동작**을 구분합니다.

## 자동 검증

- TypeScript 엄격 모드 타입 검사 및 자동 테스트 22개.
- Vitest + Happy DOM: 어댑터 탐색, 서버 순서·선택·읽지 않은 표시, 읽기 전용 대화, 인라인 편집기와 채팅 입력기 구분.
- 이미지 기본 접힘·개별 확장·전체 접기, 스포일러 미공개 유지, 가상 목록 노드 재삽입 시 상태 유지, 대화 이동 시 초기화.
- 유니코드·커스텀 이모지 대체, 복합 이모지·국기·키캡, 코드 보존, 원래 텍스트 노드·링크 보존, 반응 클릭 전달과 상태 갱신, 중복 표시층 방지.
- 새 메시지 처리, 로컬 행 번호 안정성, 채널 이동, 시트 클릭의 최신 원본 노드 사용.
- 비활성화 후 원래 DOM의 정확한 복원, 작성 중 텍스트 노드 보존, IME 조합 Enter와 Shift+Enter 이벤트를 가로채지 않음, 관찰 처리 중지.
- 구조 불일치 후 자동 복원·재시도, 설정 유지·재활성화, 미지 설정 필드 제거, 표시 설정 외 데이터 미저장, 최소 권한 선언.

실제 OS의 한글 IME를 재현한 테스트가 아니라 **조합 상태 키 이벤트가 방해받지 않는지** 확인한 테스트입니다.

## 브라우저에서 확인한 샘플 화면

Codex 내장 Chromium 브라우저, 1280 × 720 기준:

- 엑셀 리본·수식 입력줄·열 문자·시간·작성자·메시지 격자·하단 서버 시트 표시.
- 네이티브 입력줄이 헤더에 가려지지 않고 열 머리글과 겹치지 않음.
- 이미지 확장 높이 225px, 다음 메시지 행과 겹침 없음.
- 이모지 숨김 상태의 반응 클릭에서 `:thumbsup: 3` → `:thumbsup: 4` 확인.
- 전체 이미지 접기와 실제 샘플 검색 입력란의 표시·포커스 이동.
- 한글 두 줄 입력, Shift+Enter에서 전송하지 않음, Enter에서 두 줄 메시지 한 건 추가 및 입력줄 비워짐.
- DM 시트 이동, 왼쪽 채널 창 접기.
- 원래 화면 복원 시 추가 행·이모지·이미지 속성 및 도구 모음 제거, 원래 서버 탐색과 입력기 표시.
- 서버 시트 21개에서 가로 스크롤, 과거 메시지 추가 후 18개 행에서 겹침 없음.
- 화면 구조 변경 재현 시 자동으로 원래 화면 복원 및 재시도 안내 표시.

샘플 입력·반응은 로컬 DOM만 변경하며, 실제 대화 상대에게 보내지 않았습니다.

## 실제 계정 검증: 보류

`discord.com`을 브라우저로 열려는 단계에서 자동 승인 검토가 접근을 차단했습니다. 로그인된 계정과 비공개 Discord 콘텐츠를 브라우저 연결이 읽을 수 있는 권한 확대라는 이유였습니다. 다른 수단으로 우회하지 않았습니다.

따라서 아래 항목은 완료했다고 주장하지 않습니다.

- 현재 로그인된 실제 Discord DOM에 대한 선택자 호환성 및 실제 스크롤 가상화와의 상호작용.
- 크롬·엣지에 압축 해제 확장을 실제로 설치한 상태에서의 실행.
- 실제 계정의 서버·DM 수신·전송·답장·멘션·첨부·반응.
- Windows 한글 IME를 이용한 실제 입력, Discord 이미지 원본 보기·GIF 재생·스포일러 UI.
- 실제 Discord 업데이트 전후 복원 및 다시 적용.

다음 검증에는 사용자의 명시적인 `discord.com` 브라우저 접근 승인과 로그인된 페이지가 필요합니다. 실제 전송 테스트는 사용자가 지정하고 승인한 테스트 대화·문구에서만 진행합니다.
